# ELP-DSP-00 retained evidence — 2026-08-27

## Result and claim boundary

Status: **foundation implemented; local framework, source-attested, contract,
backend, and deterministic-catalog validation pass; frontend installation and
exact-PR-head hosted CI remain open gates**.

This record supports a read-only DSP/Radar source pin, an exact 84-item
source-to-target inventory, a fail-closed conversion framework, and a native
catalog entry with zero converted DSP modules. It does not establish a
converted P01-P84 lesson, Python numerical equivalence, MATLAB execution,
browser or accessibility acceptance, learner effectiveness, hardware behavior,
release, deployment, or production operation.

## Provenance and exact identities

- Target repository: `tranquilWorks/engineering-learning-platform`.
- Target baseline, pre-edit `HEAD`, `origin/main`, and `origin/HEAD`:
  `8019d1798ea771f1f466e24c9983549ec1d6c127`.
- Branch: `agent/elp-dsp-00-foundation`.
- Merged control-plane contract revision:
  `b6bb6b115722739bb927660b967432f89c5c101d`.
- Activated contract remote Git blob:
  `9d6529fb8290297806aff0582bff54a8dbe48e12`.
- DSP gitlink: `203c3738a88070bd43a93d969b6991a195bb6e27`
  to `5d73667a486df4a7b6c581e4c9406e810ed4f0f6`, mode `160000`.
- Source tree: `7a3a0f9adce607e10097724c13745eace212f4e1`.
- Source curriculum SHA-256:
  `0b92e76efc1f72930fab730145326315219ab3813b8cbf17d32a33f507a4974f`.
- Source 420-file aggregate SHA-256:
  `c2511015b195a48bd847bd1f1cdeed92384a10eddf0b23e755590af4fb66ddca`.
- `.gitmodules` SHA-256 remained
  `a2739650b61500f05b3331fa69f1a4c417f244b5aa55b06d25aac68fb352c792`.
- Python: `3.12.13`; Node: `v24.19.0`; npm: `11.9.0`.

Framework file identities:

| File | SHA-256 |
|---|---|
| `course.yaml` | `9950220969a64e5c7faed96ae1f8a2395339c7dbd292a9471c89001a2a5d0228` |
| `source-map.yaml` | `5b2751769cf70d7c4faad148fecd2db16e7995982122777fa012bfbda7850bf5` |
| `conversion-manifest.yaml` | `1244c6dea11ea11b880e0f26d54ce81dea42ea1ab4981d4d5180c440eb7890a4` |
| `coverage.yaml` | `17d396d21b0298921e7a2d6408efc1b1fb7c287fb38430d5708ce373f61fa718` |
| `conversion.schema.json` | `8d728f2158c95944ff7ee3ba599bfdddb91e937099d94c8b601c2a43bd1ea73f` |
| `AUTHORING.md` | `e4eb5b356791e480e82306c079961d91a4e4dc5cd1466fc23317a8915b32b8db` |

`source-map.yaml` retains every canonical path and SHA-256 for the five inputs
of each P01-P84 item. Its 84 folders and 420 file identities reproduce the
aggregate source digest above.

## Acceptance observations

- The source manifest contains P01 through P84 exactly once, numbered and
  ordered 1 through 84, all `implemented`.
- Phase counts are `10, 10, 8, 14, 10, 8, 8, 6, 10`.
- Coverage is exactly `pending=84`, `converted=0`, `blocked=0`,
  `placeholder=0`.
- `courses/dsp-radar/modules/` does not exist. No learner page, Python runtime,
  copied MATLAB file, TODO shell, or bulk placeholder was created.
- The catalog observes three courses, two implemented modules, and two
  interactive modules. DSP/Radar is present as an honest zero-module
  foundation entry.
- All other twelve course gitlinks, `.gitmodules`, the generic API/runtime,
  React application, generated schemas/types, dependencies, and existing
  courses remain unchanged.
- The focused validator requires later converted items to retain real target
  bytes, native manifest/runtime identity, a recomputed catalog content digest,
  text-numeric expected/actual results, passing Python/source tolerances, and
  correctly typed MATLAB/manual evidence. It rejects image-only numeric proof,
  self-comparison, missing evidence, bulk or out-of-order transitions, and
  fabricated catalog visibility.

## Commands and results

Commands ran from the target repository root.

1. Focused source-independent framework validation:

   `env -u ELP_DSP_SOURCE_ROOT PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=apps/api/src .venv/bin/python -B -m pytest -q apps/api/tests/test_dsp_conversion_framework.py`

   Exit 0: `48 passed in 12.05s`.

2. Exact-source-attested validation:

   `ELP_DSP_SOURCE_ROOT=courses/dsp-radar-learning PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=apps/api/src .venv/bin/python -B -m pytest -q apps/api/tests/test_dsp_conversion_framework.py`

   Exit 0: `48 passed in 10.64s`. The initialized source was clean, detached,
   and exact at the commit/tree above. The test recomputed all 84 identities,
   all 420 hashes, the curriculum and aggregate digests, and before/after
   source and Git-metadata snapshots.

3. Source inventory before and after verification:

   A C-locale stream of every tracked Git mode/blob/path plus each worktree
   mode/SHA-256/path and source status was hashed before and after the focused,
   quick, and attempted full runs. Both values were
   `19e9f7e9875d698f0a9c212dcce1fb1259a83be6138950d34b7f04029140d8c5`;
   source status remained empty.

4. Existing contract suite:

   `./scripts/agent-verify.sh contract`

   Exit 0: `72 passed in 9.89s`.

5. Full backend quick suite:

   `./scripts/agent-verify.sh quick`

   Exit 0: `124 passed, 3 warnings in 26.87s`. The warnings are the existing
   Starlette TestClient/httpx and FastAPI ORJSONResponse deprecations.

6. Deterministic catalog validation:

   `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=apps/api/src .venv/bin/python -B scripts/validate_courses.py --execute --deterministic --json`

   Exit 0: status `pass`, `courses=3`, `modules=2`,
   `interactive_modules=2`, `errors=[]`.

7. Focused lint and static drift:

   `.venv/bin/python -m ruff check apps/api/tests/test_dsp_conversion_framework.py`

   Exit 0: `All checks passed!`. `git diff --cached --check` also exited 0.

8. Full repository verification:

   `./scripts/agent-verify.sh full`

   Exit 2 after generated-contract validation, deterministic catalog validation,
   and all `124` backend tests passed. The fresh checkout had no
   `node_modules`, so the mandatory frontend typecheck/build did not start.
   A normal `npm install` was denied by the execution environment's network
   gate before installation. An offline no-lockfile attempt failed closed with
   `ENOTCACHED` for `@tanstack/react-table`. No package lock, dependency file,
   or tracked path was created or changed. This is an unresolved environment
   gate, not a frontend pass.

9. Exact-PR-head hosted CI:

   Not yet performed because this evidence record precedes branch publication.
   The existing GitHub Actions quality workflow must pass backend, frontend,
   and container jobs on the exact PR head. This record cannot promote a
   pending or earlier run into exact-head evidence.

## Product-data profile gates

| Gate | Disposition |
|---|---|
| Schema and deterministic serialization | Pass in focused tests |
| Canonical-source immutability | Pass; exact before/after inventory retained |
| 84-item uniqueness/completeness | Pass; P01-P84 and all 420 inputs reproduced |
| Hidden scoring or learner analytics | Not present |
| Data/database migration | Not applicable; none added |
| Backup/restore | Not applicable; repository-static state only |
| Recovery | Revert the single target change to restore the old gitlink and remove the derived framework |
| Existing service smoke | Backend/API and deterministic catalog pass; frontend/container remain hosted-CI gates |

## Changed and preserved invariants

Changed: the DSP source gitlink is advanced to the complete source revision;
the platform gains a zero-module DSP course manifest, immutable mapping,
coverage ledger, conversion/parity schema, successor authoring rules, focused
tests, and documentation.

Preserved: canonical source bytes and metadata; `.gitmodules`; the other twelve
gitlinks; existing courses and runtime results; generic platform models, API,
React code, generated types/schemas, workflows, dependencies, learner data,
release, deployment, and production state.

## Residual risks, gates, and rollback

- Conversion remains 0/84. P01 is the first permitted successor and must not
  start until its control-plane batch is materialized after this foundation.
- Python/source equivalence, MATLAB execution, browser/accessibility review,
  learner review, and the aggregate final gate are unperformed.
- Static retained evidence can verify browser/MATLAB claim shape and file
  identity but cannot prove that a human or licensed MATLAB runtime performed
  the represented action.
- Numeric retained evidence currently supports UTF-8 JSON and comma/tab-
  delimited numeric text; binary `.npy` or `.mat` evidence would require a
  reviewed validator extension.
- A blocked coverage entry binds a retained repository path but the current
  ledger contract has no blocker-evidence hash field.
- Local frontend verification remains blocked by dependency-install network
  policy, and exact-head hosted CI is pending.

Before any P## successor merges, rollback is one governed revert of the
ELP-DSP-00 target change. That restores gitlink `203c3738...` and removes the
platform-owned framework without writing the source repository or reversing a
database, release, deployment, or production action.
