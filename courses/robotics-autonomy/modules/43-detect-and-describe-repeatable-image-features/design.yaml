schema_version: 1
item_id: P43
module: 43-detect-and-describe-repeatable-image-features
provenance:
  kind: python-first-native
  source_equivalence_claimed: false
  competency_map: ../../competency-map.yaml
  matlab_runtime: not_run
governing_equations:
- R=det(M)-k trace(M)^2
- theta=atan2(m_01,m_10)
- d=||d_1-d_2||
signature:
- field: repeatability
  unit: '1'
- field: descriptor_distance
  unit: '1'
- field: feature_count
  unit: count
teaching_invariant: A useful feature remains localized and descriptively close under the declared viewpoint transformation
  while retaining spatial coverage.
limiting_cases:
- At zero image rotation, orientation-induced descriptor difference vanishes.
- As threshold rises, retained feature count cannot increase for fixed responses.
scenarios:
  baseline: &id001
    detector_threshold: 0.12
    image_rotation_deg: 25.0
    broken_mode: false
  sweep_1:
    detector_threshold: 0.8
    image_rotation_deg: 25.0
    broken_mode: false
  sweep_2:
    detector_threshold: 0.12
    image_rotation_deg: 180.0
    broken_mode: false
  broken:
    detector_threshold: 0.12
    image_rotation_deg: 25.0
    broken_mode: true
  recovery: *id001
independent_reference:
  path: ../../expansion_reference_cases.py
  imports_production: false
  derived_from_production_output: false
  perturbs_production_output: false
tolerance:
  absolute: 1.0e-08
  relative: 1.0e-08
claim_boundary: Deterministic Python software evidence only; no MATLAB runtime, learner, browser/accessibility,
  physical HIL/hardware, certification, release, deployment, or production claim.
