schema_version: 1
item_id: P39
module: 39-couple-heave-pitch-and-roll-modes
provenance:
  kind: python-first-native
  source_equivalence_claimed: false
  competency_map: ../../competency-map.yaml
  matlab_runtime: not_run
governing_equations:
- K=sum_i k_i q_i q_i^T
- K phi=lambda M phi
- f_n=sqrt(lambda)/(2 pi)
signature:
- field: first_natural_frequency
  unit: "Hz"
- field: second_natural_frequency
  unit: "Hz"
- field: third_natural_frequency
  unit: "Hz"
- field: stiffness_coupling_ratio
  unit: "1"
- field: maximum_eigen_residual
  unit: "1"
teaching_invariant: Every reported coupled mode must satisfy the full physical generalized eigen-equation, including the off-diagonal terms created by corner geometry and rate asymmetry.
limiting_cases:
- With perfectly symmetric front and rear corner pairs, roll decouples from heave and pitch.
- If all corner rates scale together, frequencies scale with the square root of that factor while mode shapes are retained.
scenarios:
  baseline:
    front_spring_rate_n_m: 42000
    rear_spring_rate_n_m: 38000
    broken_mode: false
  sweep_1:
    front_spring_rate_n_m: 56000
    rear_spring_rate_n_m: 38000
    broken_mode: false
  sweep_2:
    front_spring_rate_n_m: 42000
    rear_spring_rate_n_m: 52000
    broken_mode: false
  broken:
    front_spring_rate_n_m: 42000
    rear_spring_rate_n_m: 38000
    broken_mode: true
  recovery:
    front_spring_rate_n_m: 42000
    rear_spring_rate_n_m: 38000
    broken_mode: false
independent_reference:
  path: ../../expansion_reference_cases.py
  imports_production: false
  derived_from_production_output: false
  perturbs_production_output: false
tolerance:
  absolute: 1.0e-08
  relative: 1.0e-08
claim_boundary: Deterministic Python software and synthetic parameters only; no source or MATLAB equivalence, measured damper, suspension, rig, track, or vehicle result, learner, browser/accessibility, physical HIL/hardware, certification, release, deployment, or production claim.
