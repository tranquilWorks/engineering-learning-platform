schema_version: 1
item_id: P49
module: 49-control-a-constrained-plant-with-model-predictive-control
provenance:
  kind: python-first-native
  source_equivalence_claimed: false
  competency_map: ../../competency-map.yaml
  matlab_runtime: not_run
governing_equations:
- x_{k+1}=x_k+u_k
- J=sum(x_k-r)^2+.1u_k^2
- '|u_k|<=u_max'
signature:
- field: first_control_move
  unit: '1'
- field: constraint_activity
  unit: '1'
- field: predicted_terminal_error
  unit: '1'
teaching_invariant: The applied MPC move satisfies the declared input bound; an unconstrained optimum is not a feasible
  control law when it exceeds that bound.
limiting_cases:
- With zero initial error the optimal move is zero.
- As input limit grows, the constrained move approaches the unconstrained optimum.
scenarios:
  baseline: &id001
    prediction_horizon: 6.0
    input_limit: 0.8
    broken_mode: false
  sweep_1:
    prediction_horizon: 20.0
    input_limit: 0.8
    broken_mode: false
  sweep_2:
    prediction_horizon: 6.0
    input_limit: 2.0
    broken_mode: false
  broken:
    prediction_horizon: 6.0
    input_limit: 0.8
    broken_mode: true
  recovery: *id001
independent_reference:
  path: ../../expansion_reference_cases.py
  imports_production: false
  derived_from_production_output: false
  perturbs_production_output: false
tolerance:
  absolute: 1.0e-08
  relative: 1.0e-08
claim_boundary: Deterministic Python software evidence only; no MATLAB runtime, learner, browser/accessibility,
  physical HIL/hardware, certification, release, deployment, or production claim.
