schema_version: 1
commands:
  contract:
    - ./scripts/agent-verify.sh contract
  quick:
    - ./scripts/agent-verify.sh quick
  full:
    - ./scripts/agent-verify.sh full
evidence_boundary: These commands provide repository-static, deterministic software, API protocol, and frontend build evidence for the active batch only. They do not provide hosted-CI, browser-universality, accessibility-universality, MATLAB, hardware, release, deployment, or production evidence.
